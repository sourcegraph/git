# Agent guidance

This is Sourcegraph's downstream Git fork. Before making changes, read and
follow `SOURCEGRAPH.md`. Base downstream work on `sourcegraph`, never `master`.

Also follow `Documentation/SubmittingPatches` and
`Documentation/CodingGuidelines`.
